Процедура Проведение(Отказ, РежимПроведения)

    // Проверка заполненности
    Если НЕ ЗначениеЗаполнено(Студент) Или НЕ ЗначениеЗаполнено(Дисциплина) Тогда
        Отказ = Истина;
        Сообщить("Не заполнены обязательные поля: Студент или Дисциплина");
        Возврат;
    КонецЕсли;

    // Движение по регистру накопления "Успеваемость"
    Движение = РегистрыНакопления.Успеваемость.СоздатьНаборЗаписей();
    Движение.Отбор.Регистратор.Установить(Ссылка);

    Запись = Движение.Добавить();
    Запись.Период = Дата;
    Запись.Студент = Студент;
    Запись.Дисциплина = Дисциплина;
    Запись.Оценка = Оценка;

    Движение.Записать();

КонецПроцедуры
